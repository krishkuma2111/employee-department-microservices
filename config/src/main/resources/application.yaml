server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native

  cloud:
    config:
      server:
        git:
          enabled: false
        native:
          search-locations: file:/home/krishna/projects/microservices/config-repo
